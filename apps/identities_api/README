This app provides an HTTP API by which the GPG public key
associated with a particular username@domain can be found.

It also queries other "theapp" servers using this API.

This app depends on the gpg apps.
